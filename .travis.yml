language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-sda' ]; then cd .. && eval "mv $currentfolder generator-sda" && cd generator-sda; fi

